export function SiteFooter() {
  return (
    <footer className="mt-24 border-t border-line/80">
      <div className="mx-auto flex max-w-6xl flex-col gap-3 px-5 py-10 text-sm text-muted sm:px-8 md:flex-row md:items-center md:justify-between">
        <p>Decantos, um lugar para decantar a vida.</p>
        <p>Ensaios para ler sem pressa.</p>
      </div>
    </footer>
  );
}
